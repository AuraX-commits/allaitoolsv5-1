
import React from "react";

// Removing the ThemeToggle component since we're removing dark mode
const ThemeToggle = () => {
  // Empty component - we're removing dark mode functionality
  return null;
};

export default ThemeToggle;
